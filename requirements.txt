pymupdf>=1.24
faiss-cpu>=1.8
rank-bm25>=0.2.2
numpy>=1.26
scikit-learn>=1.3
python-dotenv>=1.0
pytest>=8.0
# sentence-transformers: optional local dense fallback (needs torch>=2.6).
# Mặc định dùng OpenRouter embeddings API (OPENROUTER_API_KEY) rồi fallback TF-IDF offline.
